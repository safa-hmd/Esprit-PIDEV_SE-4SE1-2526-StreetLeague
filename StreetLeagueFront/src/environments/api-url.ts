import { environment } from './environment';

/**
 * URL de l'API Spring Boot — récupérée depuis l'environnement Angular.
 * Importer depuis ici ou utiliser directement environment.baseUrl dans les services.
 */
export const API_BASE_URL = environment.baseUrl;
