/**
 * URL de l'API Spring Boot — fichier dédié pour éviter que fileReplacements (ng serve)
 * ne vide ou casse environment.*.ts. Importer depuis ici pour les appels HTTP.
 */
export const API_BASE_URL = 'http://localhost:8086/StreetLeague';
